/// <reference types="react" />
import { RGBColor } from '@kepler.gl/types';
interface DatasetLabelType {
    dataset: {
        color: RGBColor;
        label: string;
    };
}
declare const DatasetLabel: ({ dataset }: DatasetLabelType) => JSX.Element;
export default DatasetLabel;
