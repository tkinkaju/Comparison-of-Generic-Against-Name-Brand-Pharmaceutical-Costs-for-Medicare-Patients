/// <reference types="react" />
interface FloatingTimeDisplayProps {
    currentTime: number | number[];
    defaultTimeFormat?: string | null;
    timeFormat?: string | null;
    timezone?: string | null;
}
export default function FloatingTimeDisplayFactory(): ({ currentTime, defaultTimeFormat, timeFormat, timezone }: FloatingTimeDisplayProps) => JSX.Element;
export {};
