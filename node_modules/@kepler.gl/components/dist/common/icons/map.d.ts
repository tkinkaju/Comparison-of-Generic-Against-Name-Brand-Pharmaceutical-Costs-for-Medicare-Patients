import { Component } from 'react';
import { BaseProps } from './base';
declare class MapIcon extends Component<Partial<BaseProps>> {
    static defaultProps: {
        height: string;
        predefinedClassName: string;
        totalColor: number;
    };
    render(): JSX.Element;
}
export default MapIcon;
