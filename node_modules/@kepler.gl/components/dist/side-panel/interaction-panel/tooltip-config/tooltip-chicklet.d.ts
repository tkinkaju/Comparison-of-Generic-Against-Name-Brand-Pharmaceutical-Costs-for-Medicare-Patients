import { ComponentType } from 'react';
interface TooltipChickletProps {
    disabled: boolean;
    item: {
        name: string;
    };
    displayOption: Function;
    remove: any;
}
declare type TooltipConfig = {
    fieldsToShow: {
        [key: string]: {
            name: string;
            format: string | null;
        }[];
    };
    compareMode: boolean;
    compareType: string[];
};
declare type TooltipFields = {
    format?: string;
    id?: string;
    name?: string;
    fieldIdx?: number;
    type?: number;
};
declare function TooltipChickletFactory(dataId: string, config: TooltipConfig, onChange: (cfg: TooltipConfig) => void, fields: TooltipFields[]): ComponentType<TooltipChickletProps>;
export default TooltipChickletFactory;
