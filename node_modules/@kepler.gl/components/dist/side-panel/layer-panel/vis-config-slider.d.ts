import React from 'react';
import RangeSliderFactory from '../../common/range-slider';
import { Layer, LayerBaseConfig } from '@kepler.gl/layers';
declare type VisConfigSliderProps = {
    layer: Layer;
    property: string;
    onChange: (v: Record<string, number | string | number[] | string[]>) => void;
    label?: string | ((c: LayerBaseConfig) => string);
    range: [number, number];
    step?: number;
    isRanged: boolean;
    disabled?: boolean;
    inputTheme?: string;
    allowCustomValue?: boolean;
};
declare function VisConfigSliderFactory(RangeSlider: ReturnType<typeof RangeSliderFactory>): React.FC<VisConfigSliderProps>;
declare namespace VisConfigSliderFactory {
    var deps: (typeof RangeSliderFactory)[];
}
export default VisConfigSliderFactory;
