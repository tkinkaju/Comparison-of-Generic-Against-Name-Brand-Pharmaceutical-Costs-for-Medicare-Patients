import { ComponentType } from 'react';
import PanelHeaderActionFactory from '../panel-header-action';
import { MapStyle } from '@kepler.gl/reducers';
import { BaseProps } from '../../common/icons';
declare type MapStyleSelectorProps = {
    mapStyle: MapStyle;
    onChange: (payload: string) => void;
    toggleActive: () => void;
    isSelecting: boolean;
    actionIcons?: Record<string, ComponentType<Partial<BaseProps>>>;
};
declare function MapStyleSelectorFactory(PanelHeaderAction: ReturnType<typeof PanelHeaderActionFactory>): ({ mapStyle, onChange, toggleActive, isSelecting, actionIcons }: MapStyleSelectorProps) => JSX.Element;
declare namespace MapStyleSelectorFactory {
    var deps: (typeof PanelHeaderActionFactory)[];
}
export default MapStyleSelectorFactory;
