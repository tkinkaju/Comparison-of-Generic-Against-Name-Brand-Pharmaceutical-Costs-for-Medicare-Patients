import React from 'react';
import PanelTabFactory, { PanelItem } from './panel-tab';
import { toggleSidePanel, ActionHandler } from '@kepler.gl/actions';
declare type PanelToggleProps = {
    panels: PanelItem[];
    activePanel: string;
    togglePanel: ActionHandler<typeof toggleSidePanel>;
};
declare function PanelToggleFactory(PanelTab: ReturnType<typeof PanelTabFactory>): React.FC<PanelToggleProps>;
declare namespace PanelToggleFactory {
    var deps: (typeof PanelTabFactory)[];
}
export default PanelToggleFactory;
