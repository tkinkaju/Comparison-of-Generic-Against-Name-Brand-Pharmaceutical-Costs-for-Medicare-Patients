import { Datasets } from './kepler-table';
import { ProtoDataset, RGBColor } from '@kepler.gl/types';
/** @type {typeof import('./dataset-utils').getNewDatasetColor} */
export declare function getNewDatasetColor(datasets: Datasets): RGBColor;
/**
 * Take datasets payload from addDataToMap, create datasets entry save to visState
 */
export declare function createNewDataEntry({ info, data, ...opts }: ProtoDataset, datasets?: Datasets): Datasets;
