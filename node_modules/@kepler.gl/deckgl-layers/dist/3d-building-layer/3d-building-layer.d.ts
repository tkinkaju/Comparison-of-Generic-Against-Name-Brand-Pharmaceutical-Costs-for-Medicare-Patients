/// <reference types="deck.gl" />
/// <reference types="@danmarshall/deckgl-typings/deck.gl__layers" />
/// <reference types="@danmarshall/deckgl-typings/deck.gl__geo-layers" />
import { CompositeLayer } from '@deck.gl/core';
import { TileLayer as DeckGLTileLayer } from '@deck.gl/geo-layers';
import { ThreeDBuildingLayerProps, TileDataItem } from './types';
import { SolidPolygonLayer } from '@deck.gl/layers';
export default class ThreeDBuildingLayer extends CompositeLayer<{}, ThreeDBuildingLayerProps> {
    renderSubLayers(props: ThreeDBuildingLayerProps): SolidPolygonLayer<TileDataItem, import("@deck.gl/layers").SolidPolygonLayerProps<TileDataItem>>;
    renderLayers(): DeckGLTileLayer<TileDataItem, import("@deck.gl/geo-layers/tile-layer/tile-layer").TileLayerProps<TileDataItem>>[];
}
