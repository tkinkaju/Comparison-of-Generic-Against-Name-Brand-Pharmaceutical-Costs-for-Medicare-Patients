/**
 * Build a generic instance of credentials, eg to interact with APIs such as Windshaft or SQL
 * @param username
 * @param apiKey
 * @param serverURL A url pattern with {user}, like default 'https://{user}.carto.com'
 *
 */
export declare class Credentials {
    private _username;
    private _apiKey;
    private _serverUrlTemplate;
    constructor(username: string, apiKey: string, serverUrlTemplate?: string);
    static get DEFAULT_SERVER_URL_TEMPLATE(): string;
    static get DEFAULT_PUBLIC_API_KEY(): string;
    get username(): string;
    set username(value: string);
    get apiKey(): string;
    set apiKey(value: string);
    get serverUrlTemplate(): string;
    set serverUrlTemplate(value: string);
    get serverURL(): string;
}
export declare const defaultCredentials: Credentials;
export declare function setDefaultCredentials(credentials: {
    username: string;
    apiKey: string;
    serverUrlTemplate: string;
}): void;
//# sourceMappingURL=Credentials.d.ts.map