import { OAuth2Provider } from '@salte-auth/salte-auth';
interface CartoValidation extends OAuth2Provider.Validation {
    user_info_url: string;
}
export declare class CartoProvider extends OAuth2Provider {
    private _userInfo;
    get name(): string;
    get login(): string;
    get logout(): string;
    get userInfo(): string;
    $validate(options: CartoValidation): void;
    sync(): void;
    get expired(): boolean | undefined;
    /**
     * Returns Expiration date for current token, or -1 if already expired
     *
     * @readonly
     * @type {number}
     * @memberof CartoProvider
     */
    get expiresIn(): number;
}
export default CartoProvider;
//# sourceMappingURL=CartoProvider.d.ts.map