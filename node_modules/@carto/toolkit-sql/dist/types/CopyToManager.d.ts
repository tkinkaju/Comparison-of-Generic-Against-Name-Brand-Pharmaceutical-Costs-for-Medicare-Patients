import { Credentials } from '@carto/toolkit-core';
import RequestManager from './RequestManager';
export declare class CopyToManager extends RequestManager {
    constructor(credentials: Credentials, options?: {
        maxApiRequestsRetries?: number;
    });
    copyUrl(q: string, filename?: string, options?: string): string;
    /**
     * Copy a query or a table to stdout
     *
     * @param {string} q
     * @memberof CopyToManager
     */
    copy(q: string, filename: string, options: string): Promise<unknown>;
}
//# sourceMappingURL=CopyToManager.d.ts.map