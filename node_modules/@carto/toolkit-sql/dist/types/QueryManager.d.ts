import { Credentials, MetricsEvent } from '@carto/toolkit-core';
import RequestManager from './RequestManager';
export declare type Pair<T> = [T, T];
export declare class QueryManager extends RequestManager {
    constructor(credentials: Credentials, options?: {
        maxApiRequestsRetries?: number;
    });
    query(q: string, options?: {
        extraParams?: Array<Pair<string>>;
        event?: MetricsEvent;
    }): Promise<unknown>;
    private prepareGetRequest;
    private preparePostRequest;
}
//# sourceMappingURL=QueryManager.d.ts.map