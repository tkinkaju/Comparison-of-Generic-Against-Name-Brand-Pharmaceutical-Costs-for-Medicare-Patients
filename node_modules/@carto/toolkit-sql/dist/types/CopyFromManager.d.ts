import { Credentials, MetricsEvent } from '@carto/toolkit-core';
import RequestManager from './RequestManager';
export declare class CopyFromManager extends RequestManager {
    constructor(credentials: Credentials, options?: {
        maxApiRequestsRetries?: number;
    });
    copy(csv: string, tableName: string, fields: string[], options?: {
        event?: MetricsEvent;
    }): Promise<unknown>;
}
//# sourceMappingURL=CopyFromManager.d.ts.map