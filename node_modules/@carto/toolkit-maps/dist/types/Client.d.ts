import { Credentials } from '@carto/toolkit-core';
export declare class Maps {
    private _credentials;
    constructor(credentials: Credentials);
    /**
     * Instantiate a map based on dataset name or a sql query, returning a layergroup
     *
     * @param options
     */
    instantiateMapFrom(options: MapOptions): Promise<any>;
    private instantiateMap;
    private makeMapsApiRequest;
    private dealWithWindshaftErrors;
    private generateMapsApiUrl;
}
export interface MapOptions {
    sql?: string;
    dataset?: string;
    vector_extent: number;
    vector_simplify_extent: number;
}
//# sourceMappingURL=Client.d.ts.map