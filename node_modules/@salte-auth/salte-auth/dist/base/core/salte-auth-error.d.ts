export declare class SalteAuthError extends Error {
    code: string;
    constructor({ message, code }: SalteAuthError.Options);
}
export declare namespace SalteAuthError {
    interface Options {
        message: string;
        code: string;
    }
}
