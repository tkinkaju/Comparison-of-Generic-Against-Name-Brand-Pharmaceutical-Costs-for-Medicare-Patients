import { Handler } from '@salte-auth/salte-auth';
export declare class Popup extends Handler {
    constructor(config?: Popup.Config);
    readonly name: string;
    readonly auto: boolean;
    open(options: Handler.OpenOptions): Promise<{}>;
}
export interface Popup {
    config: Popup.Config;
}
export declare namespace Popup {
    interface Config extends Handler.Config {
        /**
         * The name to attach to the popup window.
         *
         * @default 'salte-auth'
         */
        name?: string;
        /**
         * The height of the popup window.
         *
         * @default 600
         */
        height?: number;
        /**
         * The width of the popup window.
         *
         * @default 600
         */
        width?: number;
    }
}
