import { Texture2D } from '@luma.gl/core';
import type { Effect, PreRenderOptions } from '../../lib/effect';
export default class CollideEffect implements Effect {
    id: string;
    props: null;
    useInPicking: boolean;
    private channels;
    private collidePasses;
    private dummyCollideMap?;
    private lastViewport?;
    preRender(gl: WebGLRenderingContext, { effects: allEffects, layers, layerFilter, viewports, onViewportActive, views }: PreRenderOptions): void;
    private _render;
    /**
     * Group layers by collideGroup
     * Returns a map from collideGroup to render info
     */
    private _groupByCollideGroup;
    getModuleParameters(): {
        collideMaps: Record<string, Texture2D>;
        dummyCollideMap: Texture2D;
    };
    cleanup(): void;
    _debug(collidePass: any): void;
}
//# sourceMappingURL=collide-effect.d.ts.map